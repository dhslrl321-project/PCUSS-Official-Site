import Modal from ".";

export default {
  title: "organisms / Modal",
  component: Modal,
};

const Template = (args) => <Modal {...args} />;

export const Rendering = Template.bind({});
Rendering.args = {};
