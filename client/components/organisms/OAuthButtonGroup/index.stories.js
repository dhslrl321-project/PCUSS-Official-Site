import OAuthButtonGroup from ".";

export default {
  title: "organisms / OAuthButtonGroup",
  component: OAuthButtonGroup,
};

const Template = (args) => <OAuthButtonGroup {...args} />;

export const Default = Template.bind({});

Default.args = {};
