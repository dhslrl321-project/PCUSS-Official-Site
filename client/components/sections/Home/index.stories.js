import Home from ".";

export default {
  title: "sections / Home",
  component: Home
}

const Template = (args) => <Home {...args} />

export const Rendering = Template.bind({});
Rendering.args = {

}